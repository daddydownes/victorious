# Two ending choices

Baseline a97b3b6fb49196c02484672912b019252ab9644a. Back to the vault returns directly to the archive through its existing landing/focus/native-pan lifecycle, without navigation or replaying the opening. Refresh website retains the existing full reload. Matching unboxed gold controls share the black ending; no new email form.

The guided controller resets its media readiness, arrival mark, scroll state and vault gate before invoking the root return. On the next Surface, the existing film/heading/V arrival and input gate run again; cached media can be reused. The actual game and all media files are unchanged.

Sixteen consecutive return loops pass: four each Chromium desktop, WebKit phone, Chromium320px narrow and reduced-motion WebKit. Each covers Surface, preview activation, game/Exit, vault return and another Surface; rapid duplicate return clicks and keyboard activation are included. Vault return produces no document navigation, restores focus and leaves film paused. All four lanes confirm Refresh restarts the original root opening. Both controls fit viewport bounds with no horizontal overflow. Original source/reveal/Surface and106game-lifecycle checks pass.

Limits: Windows browser engines and emulated devices; no physical Safari certification. Signup transport intercepted; no inbox sends. Independent review is recorded separately.
