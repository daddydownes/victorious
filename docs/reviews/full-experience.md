# Complete-experience candidate review

Base: upstream `e972c19`. Candidate remains local and unpublished.

The September 7 [release sprint review](release-sprint.md) supersedes earlier ending and button descriptions below. It records the painted vault invitation, image failure fallbacks, mobile spacing, stable action targets and dynamic reduced-motion correction, with the expanded browser matrix.

## Executed checks

- `node tests/full-experience.cjs`: 13 passes. Scripts parse; opening/Surface V-and-star matches the root source, background wordmark and six photo references match accepted source; navigation, denied-storage restart, gate resize, end-only replay, transformed panel bounds and constant video dimensions across four viewport fixtures, assets and byte-identical regeneration pass.
- `node tests/reveal.cjs`: normal/reduced-motion cases pass. `node tests/surface-input.cjs`: 50 interaction cases pass. `git diff --check`: no whitespace errors. Other mandatory tests passed earlier before final Surface and film-control edits; they are not browser evidence.
- FFprobe: 33.1 seconds, 1280×720 H.264, no audio. HTTP page/state 200 and video range 206 checked. Continuous source second 4 through end; no montage cuts.
- Desktop in-app browser: original V-and-star and final animated Scroll label visually inspected (tagline removed); cue reaches statement; film plays with Replay hidden. Panel expanded from approximately 491×548 to 1063×605 while video layout stayed 1063×605. Playback advanced during scrolling. Replay became clickable at completion; clicking restarted at approximately 0.25 seconds and hid the control.
- Browser game entry displayed the fullscreen dialog. Exit closed it, restored body scrolling and focused Play. Back to the vault opened the original draggable archive. Surface landed on the story; after the final brand correction, both opening and Surface use the source V-and-star; the bridge layer was visible above the original stage. Restart reached the original PRESS HERE invitation after the opening.

## Limits

Video repaint was inferred from per-frame geometry updates; fixed layout dimensions are observed after the change. Spot screenshots and layout samples are not a frame-by-frame compositor audit. Black source cards at approximately 1.93–3.23 seconds and 30.1 seconds onward in the trimmed film remain. No universal absence of flicker is claimed.

The QA tab retained one console error from a draft before this final video integration; that historical entry alone does not establish a current error. No physical phone, Safari, full responsive screenshot matrix or real email submission was exercised. Reduced-motion and alternate viewport geometry were checked mechanically. This candidate retains the accepted story game; migration of the newer root four-stage game is deferred.

## Consolidated review decisions

Applied: original V-and-star opening identity, visible invitation, film replacing main photo, original Portrait Depth motion, end-only Replay, real vault/restart links, storage-independent restart, gate resize fix, whole-card game entry, and refresh deferral during scrolling/playback. Preserved: exact identity, six photos, original root assets/metadata/hosting and snapshots. Deferred: unrelated gallery redesigns, signup transport changes and broader game redesign. Thirty earlier read-only specialist reports were consolidated; the final browser-resilience review added video-layout and source-title-card findings.

## Post-film follow-up

September 7 minimal-cue validation: browser play-copy text is exactly “FLY THE V.” and “PLAY THE GAME”. At 629px the arrow uses point-down-to-game with the downward path visible; at 1164px it uses point-to-game with only the right path visible. Neither viewport overflows horizontally. Native label activation opens the game and exits with journey-play focused; preview activation opens the game and exits with flapPlay focused and body overflow visible. The missing best-line node is guarded by the existing engine; the runtime reduced-motion explanation was also removed so it cannot add extra copy. Reduced-motion CSS disables the arrow animation. Existing thirteen integration checks pass. These checks do not establish a production release or physical Safari validation.

September 7 scroll-paint validation: actual browser at 629x961 showed partial FL at progress .239 and scrollY 2410.909, FLY plus part of the second line at .602 and scrollY 2602.727, then full original artwork at 1.000 and scrollY 2856.364. Canvas was removed at completion. Reverse scroll to 2638.182 retained 1.000. Title, copy and Play CTA share centre x307.727; no horizontal overflow. Desktop 1164x818 retained two columns; narrow 391x818 stacked with no overflow. Game opened, exited, restored journey-play focus and body overflow visible. The run-local paint lifecycle fixture covers reverse/fast scroll, game suspension, idle scheduling, delayed-load focus/reduced-motion and unavailable-canvas fallback; all passed. Thirteen existing integration checks and deterministic regeneration passed. These are browser and simulated lifecycle checks, not a physical phone/Safari or frame-by-frame performance certification.

Thirteen checks now pass, including repeat 100-point unlocks and finite surprise rendering in portrait, desktop and short landscape fixtures. New Play button browser exit restored focus to journey-play and removed the scroll lock. A separate local browser fixture invoked the real win handler at synthetic score 100 and visually rendered the 100 Club panel; it does not prove a real full flight. The memory passage text/progress changes during real browser scrolling. The Fly the V headline now uses a real transparent PNG with legible aerosol-painted strokes, narrow overspray and drips. Browser compositing on black confirmed no halo; the raw preview displayed transparent RGB haze that is absent in the actual browser rendering. Original alpha is preserved. Decorative divider elements are hidden, and text underline pseudo-elements are removed.

Final focus correction: cold review found the inherited vault focus-visible shadow winning over the base no-shadow rule. The final override now includes focus-visible. Real keyboard Tab inspection before the fix showed 22px inset and 26px outer shadows; after the fix, computed box-shadow is none while the 1px gold outline and 3px offset remain. Thirteen integration checks pass after regeneration.
## Painted invitation follow-up

Browser screenshots at629x960 and1280x960 show PLAY THE GAME as the painted heading and no visible small typed label. The raster arrow points down in stacked layout and right in desktop columns. Partial heading/arrow paint and completed states were inspected. Both Play controls open the game and restore their own focus on exit. Reduced-motion emulation reports no mask animation and zero stroke-dashoffset. No page errors or horizontal overflow in these checks; restored film loop remains false.13 integration checks pass. PNG IHDRs are RGBA. This is local demo validation, not a physical-device certification.

Direct junction check: at629x960 and1280x960 the next DOM sibling after play is vault-invite, zero memory-passage elements and none of the removed phrases in main text. Screenshots show the direct junction, with no overflow or page errors. The painted arrow button still opens the game, exit restores journey-play focus, and the vault link retains ../#vault.13 integration checks pass after removal.
